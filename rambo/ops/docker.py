def up(cmd, params):
    print("up in docker")

def destroy(cmd, params):
    print("up in docker")

def halt(cmd, params):
    print("up in docker")

def ssh(cmd, params):
    print("up in docker")

def scp(cmd, params):
    print("up in docker")
