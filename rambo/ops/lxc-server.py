def up(cmd, params):
    print("up in lxc")

def destroy(cmd, params):
    print("up in lxc")

def halt(cmd, params):
    print("up in lxc")

def ssh(cmd, params):
    print("up in lxc")

def scp(cmd, params):
    print("up in lxc")
