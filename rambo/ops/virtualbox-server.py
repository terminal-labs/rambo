def up(cmd, params):
    print("up in virtualbox")

def destroy(cmd, params):
    print("destroy in virtualbox")

def halt(cmd, params):
    print("halt in virtualbox")

def ssh(cmd, params):
    print("ssh in virtualbox")

def scp(cmd, params):
    print("scp in virtualbox")
