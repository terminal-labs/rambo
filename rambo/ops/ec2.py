def up(cmd, params):
    print("up in ec2")

def destroy(cmd, params):
    print("up in ec2")

def halt(cmd, params):
    print("up in ec2")

def ssh(cmd, params):
    print("up in ec2")

def scp(cmd, params):
    print("up in ec2")
